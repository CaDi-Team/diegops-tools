//! Git identity setup.

/// Sets git global user.name and user.email.
pub fn set(_name: Option<&str>, _email: Option<&str>) -> Result<(), Box<dyn std::error::Error>> {
    todo!("devtools git set")
}
